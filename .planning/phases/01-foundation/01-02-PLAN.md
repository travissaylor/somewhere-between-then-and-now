---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/page.tsx
  - src/components/canvas/Scene.tsx
  - src/components/scroll/ScrollEngine.tsx
  - src/components/debug/DebugOverlay.tsx
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-06

must_haves:
  truths:
    - "App loads in browser without SSR hydration errors in console"
    - "Scrolling the page updates globalProgress from 0 to 1 visible in debug overlay"
    - "currentEra increments from 0 through 12 as user scrolls through full page"
    - "eraProgress cycles 0 to 1 within each era visible in debug overlay"
    - "Major transition eras (weight > 1) occupy visibly more scroll distance"
    - "R3F canvas is fixed-position and covers viewport at desktop and tablet widths"
    - "Scroll is smooth with Lenis inertia — no jank or jitter"
  artifacts:
    - path: "src/components/scroll/ScrollEngine.tsx"
      provides: "Lenis + GSAP ScrollTrigger integration, writes to Zustand store"
      contains: "autoRaf: false"
    - path: "src/components/canvas/Scene.tsx"
      provides: "R3F Canvas with position:fixed, ssr:false dynamic import"
      contains: "pointerEvents"
    - path: "src/app/page.tsx"
      provides: "Page with scroll container + dynamic Scene import"
      contains: "dynamic"
    - path: "src/components/debug/DebugOverlay.tsx"
      provides: "Dev-only overlay showing globalProgress, currentEra, eraProgress"
  key_links:
    - from: "src/components/scroll/ScrollEngine.tsx"
      to: "src/store/eraStore.ts"
      via: "eraStore.setState() in ScrollTrigger onUpdate"
      pattern: "eraStore\\.setState"
    - from: "src/components/scroll/ScrollEngine.tsx"
      to: "src/lib/scrollMath.ts"
      via: "computeEraProgress called with self.progress"
      pattern: "computeEraProgress.*progress"
    - from: "src/app/page.tsx"
      to: "src/components/canvas/Scene.tsx"
      via: "next/dynamic with ssr: false"
      pattern: "dynamic.*ssr.*false"
    - from: "src/components/debug/DebugOverlay.tsx"
      to: "src/store/eraStore.ts"
      via: "useEraStore hook for reactive display"
      pattern: "useEraStore"
---

<objective>
Wire the scroll engine, R3F canvas, and debug overlay into a working browser experience where scrolling produces verified era progress values.

Purpose: This is the integration plan — connecting the tested pure logic (Plan 01) to the browser via Lenis smooth scroll, GSAP ScrollTrigger, and the R3F canvas. The debug overlay proves everything works.
Output: A scrollable page where the debug overlay shows globalProgress, currentEra, and eraProgress updating in real-time.
</objective>

<execution_context>
@/Users/tsaylor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tsaylor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor uses these directly -->

From src/config/eras.ts:
```typescript
export interface EraConfig {
  id: number;       // 0-indexed (0-12)
  name: string;
  weight: number;   // relative scroll distance
}
export const ERA_CONFIG: EraConfig[];
```

From src/lib/scrollMath.ts:
```typescript
export function computeEraProgress(globalProgress: number): {
  currentEra: number;
  eraProgress: number;
};
export function computeScrollHeight(baseVh?: number): number;
```

From src/store/eraStore.ts:
```typescript
interface EraState {
  globalProgress: number;
  currentEra: number;
  eraProgress: number;
}
export const eraStore: StoreApi<EraState>;       // vanilla — use getState()/setState()
export const useEraStore: <T>(selector: (state: EraState) => T) => T;  // React hook
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScrollEngine, Scene, page layout, and debug overlay</name>
  <files>src/components/scroll/ScrollEngine.tsx, src/components/canvas/Scene.tsx, src/app/page.tsx, src/components/debug/DebugOverlay.tsx</files>
  <action>
1. Create `src/components/scroll/ScrollEngine.tsx` ('use client'):
   - Import Lenis, gsap, ScrollTrigger, eraStore, computeEraProgress, computeScrollHeight
   - Register ScrollTrigger plugin
   - In useEffect:
     a. Create Lenis instance with `autoRaf: false`, `lerp: 0.08`, `duration: 1.2`
     b. Wire `lenis.on('scroll', ScrollTrigger.update)`
     c. Add GSAP ticker callback: `gsap.ticker.add((time) => lenis.raf(time * 1000))`
     d. Set `gsap.ticker.lagSmoothing(0)`
     e. Create ScrollTrigger with `start: 'top top'`, `end: 'bottom bottom'`, `onUpdate: (self) => { ... }` that calls `computeEraProgress(self.progress)` and writes result + globalProgress to `eraStore.setState()`
     f. Cleanup: remove ticker callback, destroy Lenis, kill all ScrollTriggers
   - Add debounced resize handler that calls `ScrollTrigger.refresh()` (pitfall 5 from research)
   - Return null (purely behavioral component)

2. Create `src/components/canvas/Scene.tsx` ('use client'):
   - R3F Canvas with styles: `position: 'fixed', top: 0, left: 0, width: '100%', height: '100vh', pointerEvents: 'none'`
   - Inside Canvas: a simple placeholder mesh (e.g., a dark box or nothing) — future phases add real content
   - Add basic camera setup (perspective, fov adjustable for tablet — use a simple approach like checking window.innerWidth > some breakpoint for FOV)

3. Create `src/app/page.tsx` ('use client'):
   - Import ScrollEngine component directly (it's 'use client', no SSR issue since it returns null)
   - Dynamic import Scene with `ssr: false` and `loading: () => null`
   - Compute scroll container height using `computeScrollHeight()` and set as CSS custom property or inline style
   - Structure:
     ```
     <ScrollEngine />
     <div style={{ height: `${scrollHeight}vh`, position: 'relative' }}>
       {/* future HTML content */}
     </div>
     <Scene />
     <DebugOverlay />
     ```
   - Note: ScrollEngine must be inside the scroll context (after Lenis can find the scroll container)

4. Create `src/components/debug/DebugOverlay.tsx` ('use client'):
   - Only render in development (`process.env.NODE_ENV === 'development'`)
   - Use `useEraStore` with selectors for reactive updates
   - Display fixed-position overlay (bottom-left corner, semi-transparent dark background) showing:
     - globalProgress (3 decimal places)
     - currentEra (integer) + era name from ERA_CONFIG
     - eraProgress (3 decimal places)
     - A simple visual bar for globalProgress
   - Style with inline styles (no Tailwind, no CSS modules needed for debug)

5. Ensure responsive layout works:
   - The scroll container and canvas should work at desktop (1024px+) and tablet (768px+) widths
   - Canvas covers full viewport at both sizes
   - No horizontal scrollbar at any supported width
   - Add a viewport meta tag if not already present in layout.tsx

CRITICAL per user decisions:
- `autoRaf: false` on Lenis — GSAP ticker drives Lenis (single RAF loop)
- `pointerEvents: 'none'` on Canvas — let scroll events pass through
- Dynamic import with `ssr: false` for Scene — prevent SSR hydration errors
- All Three.js/R3F imports behind 'use client' boundary
- Do NOT use Drei ScrollControls — GSAP ScrollTrigger is sole scroll authority
- Read store via `eraStore.getState()` in useFrame contexts, `useEraStore` hook in React components
  </action>
  <verify>
    <automated>cd /Users/tsaylor/projects/somewhere-between-then-and-now && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>App builds without errors. All four components created with correct patterns: ScrollEngine uses autoRaf:false + GSAP ticker, Scene uses position:fixed + pointerEvents:none + ssr:false, page has scroll container with computed height, DebugOverlay shows era progress values reactively.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify scroll engine works end-to-end in browser</name>
  <action>
Human verifies the complete scroll-to-era-progress engine with Lenis smooth scroll, GSAP ScrollTrigger, Zustand store, R3F canvas, and debug overlay. Scrolling the page should produce smoothly updating progress values visible in the debug overlay.

Steps to verify:
1. Run `npm run dev` in the project directory
2. Open http://localhost:3000 in Chrome
3. Open browser DevTools console — verify NO SSR hydration errors, no "window is not defined" errors
4. Look for the debug overlay in the bottom-left corner showing globalProgress, currentEra, eraProgress
5. Scroll down slowly — verify:
   - globalProgress increases from 0.000 toward 1.000
   - currentEra increments from 0 through 12
   - eraProgress cycles 0 to 1 within each era
   - Scroll feels smooth (Lenis inertia active)
6. Scroll all the way to the bottom — globalProgress should be ~1.000, currentEra should be 12
7. Scroll back up — values decrease correctly
8. Resize browser to tablet width (~768px) — canvas should still cover viewport, no layout breakage
9. Check that the R3F canvas is visible (black background or placeholder geometry) behind the scroll container
  </action>
  <verify>Human confirms all 9 verification steps pass</verify>
  <done>User has approved the scroll engine behavior: progress values update correctly, no SSR errors, smooth scroll active, responsive layout works</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors (no SSR issues)
2. `npm run dev` — app loads at localhost:3000 without console errors
3. Debug overlay shows updating progress values during scroll
4. Smooth scroll behavior via Lenis (no jank)
5. Canvas is fixed-position at desktop and tablet widths
</verification>

<success_criteria>
- Zero SSR hydration errors in browser console
- Scrolling produces globalProgress 0-1, currentEra 0-12, eraProgress 0-1 visible in debug overlay
- Major transition eras occupy more scroll distance (visible by watching currentEra change speed)
- R3F canvas is position:fixed covering viewport at desktop and tablet widths
- Lenis smooth scroll provides smooth inertia on scroll
- All downstream systems can read from Zustand store without coupling to scroll mechanics
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
