---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/config/eras.ts
  - src/lib/scrollMath.ts
  - src/lib/scrollMath.test.ts
  - src/store/eraStore.ts
  - src/store/eraStore.test.ts
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "Next.js app bootstraps and builds without errors"
    - "Era config defines 13 eras with per-era scroll weights"
    - "computeEraProgress(0) returns era 0, eraProgress 0"
    - "computeEraProgress(1) returns era 12, eraProgress ~1"
    - "Major transition eras (Divorce, Breakup) occupy more normalized scroll distance than connective eras"
    - "Era 10 (Chaos) occupies less scroll distance than Era 8 (Breakup)"
    - "Zustand store holds globalProgress, currentEra, eraProgress and updates via setState"
  artifacts:
    - path: "src/config/eras.ts"
      provides: "ERA_CONFIG array with 13 eras and scroll weights"
      exports: ["EraConfig", "ERA_CONFIG"]
    - path: "src/lib/scrollMath.ts"
      provides: "Pure functions for weighted scroll-to-era progress decomposition"
      exports: ["computeEraProgress", "computeScrollHeight"]
    - path: "src/store/eraStore.ts"
      provides: "Zustand vanilla store for era progress state"
      exports: ["eraStore", "useEraStore"]
    - path: "src/lib/scrollMath.test.ts"
      provides: "Unit tests for scroll math"
    - path: "src/store/eraStore.test.ts"
      provides: "Unit tests for Zustand store"
  key_links:
    - from: "src/lib/scrollMath.ts"
      to: "src/config/eras.ts"
      via: "import ERA_CONFIG"
      pattern: "import.*ERA_CONFIG.*from.*config/eras"
    - from: "src/store/eraStore.ts"
      to: "zustand"
      via: "createStore from zustand"
      pattern: "createStore"
---

<objective>
Bootstrap the Next.js project with all dependencies, create the era configuration, scroll math engine, and Zustand store — the pure-logic foundation that all browser-facing code depends on.

Purpose: Establish the project scaffold and testable core logic before any browser integration. The scroll math and store are consumed by every subsequent phase.
Output: A building Next.js app with tested scroll math, era config, and Zustand store ready for wiring.
</objective>

<execution_context>
@/Users/tsaylor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tsaylor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js project and install all dependencies</name>
  <files>package.json, tsconfig.json, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, vitest.config.ts</files>
  <action>
1. Bootstrap Next.js project in the current directory (which is the repo root — it already has .git and .planning). Use:
   ```
   npx create-next-app@latest . --typescript --app --no-tailwind --yes
   ```
   If create-next-app refuses to run in a non-empty directory, create in a temp dir and move files. Ensure App Router is selected (not Pages Router).

2. Install runtime dependencies:
   ```
   npm install three @react-three/fiber gsap lenis zustand
   npm install @types/three
   ```

3. Install dev dependencies for testing:
   ```
   npm install -D vitest @vitest/ui
   ```

4. After install, verify Three.js version aligns with R3F peer range:
   ```
   npm ls three
   ```
   Pin if needed.

5. Create `vitest.config.ts` at project root:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import { resolve } from 'path';

   export default defineConfig({
     test: {
       environment: 'node',
     },
     resolve: {
       alias: {
         '@': resolve(__dirname, './src'),
       },
     },
   });
   ```

6. Add test script to package.json: `"test": "vitest run"`, `"test:watch": "vitest"`

7. Update `src/app/globals.css` to include critical base styles:
   - `html, body { height: 100%; margin: 0; overflow-x: hidden; }`
   - `body { background: #000; color: #fff; }` (dark background — this is a cinematic piece)

8. Update `src/app/layout.tsx` to be a minimal RootLayout with metadata (title: "Somewhere Between Then and Now"). Include `'use client'` only if needed — layout can be a Server Component if it just wraps children.

9. Verify the app builds: `npm run build` should complete without errors.

IMPORTANT:
- Do NOT install or use Tailwind CSS (--no-tailwind flag).
- Do NOT install Drei yet — not needed for Phase 1.
- Use App Router (not Pages Router) per user decision.
- Next.js 16 uses Turbopack by default — do not override.
  </action>
  <verify>
    <automated>cd /Users/tsaylor/projects/somewhere-between-then-and-now && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js app builds successfully. package.json contains three, @react-three/fiber, gsap, lenis, zustand as dependencies. vitest is installed. globals.css has dark background and overflow-x hidden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create era config, scroll math engine, and Zustand store with tests</name>
  <files>src/config/eras.ts, src/lib/scrollMath.ts, src/lib/scrollMath.test.ts, src/store/eraStore.ts, src/store/eraStore.test.ts</files>
  <behavior>
    scrollMath tests:
    - computeEraProgress(0) returns { currentEra: 0, eraProgress: 0 }
    - computeEraProgress(1) returns { currentEra: 12, eraProgress: 1 } (or very close to 1)
    - computeEraProgress(0.5) returns a valid era between 0-12 with eraProgress between 0-1
    - computeEraProgress(-0.1) clamps to era 0, progress 0
    - computeEraProgress(1.5) clamps to era 12, progress 1
    - Era boundaries sum to approximately 1.0 (normalized weights)
    - Era with weight 3.0 (The Breakup, id 8) occupies more scroll distance than era with weight 0.5 (Year of Chaos, id 10)
    - All 13 eras are reachable by sweeping progress 0 to 1
    - computeScrollHeight returns a positive number

    eraStore tests:
    - Initial state is globalProgress: 0, currentEra: 0, eraProgress: 0
    - setState updates values and getState reads them back
    - Multiple rapid setState calls reflect latest values
  </behavior>
  <action>
1. Create `src/config/eras.ts` with the `EraConfig` interface and `ERA_CONFIG` array of 13 eras with scroll weights as specified in RESEARCH.md Pattern 4. Export both the interface and the array.

2. Create `src/lib/scrollMath.ts` implementing:
   - Pre-computed `eraBoundaries` array from ERA_CONFIG weights (computed at module load)
   - `computeEraProgress(globalProgress: number): { currentEra: number; eraProgress: number }` — clamps input to [0,1], finds era via boundary lookup, computes normalized era progress
   - `computeScrollHeight(baseVh?: number): number` — returns total scroll height in vh units based on total weight
   - Export `eraBoundaries` for test inspection

3. Create `src/lib/scrollMath.test.ts` with tests per the behavior block above. Write tests FIRST, verify they fail, then ensure the implementation passes them.

4. Create `src/store/eraStore.ts` implementing:
   - Zustand vanilla store via `createStore` (NOT `create`) with `EraState` interface
   - `eraStore` export (vanilla store instance)
   - `useEraStore` export (React hook bridge using `useStore`)
   - EraState interface: `{ globalProgress: number; currentEra: number; eraProgress: number }`

5. Create `src/store/eraStore.test.ts` with tests per behavior block.

IMPORTANT per user decisions:
- Use `createStore` from 'zustand', NOT `create` — vanilla store pattern for useFrame safety
- The `useEraStore` hook takes a selector function: `<T>(selector: (state: EraState) => T) => T`
- ERA_CONFIG weights are scaffold values — comment that authorial tuning is deferred
- Era ids are 0-indexed (0-12), 13 total
  </action>
  <verify>
    <automated>cd /Users/tsaylor/projects/somewhere-between-then-and-now && npx vitest run</automated>
  </verify>
  <done>All scrollMath and eraStore tests pass. computeEraProgress correctly maps 0-1 global progress to era-aware values respecting per-era weights. Zustand vanilla store reads and writes correctly. Era 8 (Breakup, weight 3.0) occupies more scroll distance than Era 10 (Chaos, weight 0.5).</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx vitest run` — all tests pass
3. `npm ls three @react-three/fiber gsap lenis zustand` — all dependencies installed at correct versions
</verification>

<success_criteria>
- Next.js 16 project builds cleanly with all required dependencies
- 13-era config with per-era scroll weights exists and is importable
- Scroll math correctly decomposes global progress into era-aware values (tested)
- Zustand vanilla store provides getState/setState for era progress (tested)
- Vitest infrastructure is operational
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
