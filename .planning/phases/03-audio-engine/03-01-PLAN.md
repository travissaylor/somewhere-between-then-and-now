---
phase: 03-audio-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/audioMath.ts
  - src/audio/audioMath.test.ts
  - src/audio/audioZones.ts
autonomous: true
requirements:
  - AUDO-02
  - AUDO-03
  - AUDO-04

must_haves:
  truths:
    - "computeGainForEra returns 1.0 for the current era outside overlap region"
    - "computeGainForEra returns 0.0 for eras not adjacent to current"
    - "During overlap region, current era fades in from 0 to 1 and previous era fades out from 1 to 0"
    - "Overlap width is configurable per era via AudioZoneConfig"
    - "All 13 audio zones have defined stem URLs and overlap widths"
  artifacts:
    - path: "src/audio/audioMath.ts"
      provides: "Pure gain computation for scroll-reactive audio mixing"
      exports: ["computeGainForEra", "AudioZoneConfig"]
    - path: "src/audio/audioMath.test.ts"
      provides: "Unit tests for gain computation covering all edge cases"
      min_lines: 40
    - path: "src/audio/audioZones.ts"
      provides: "13 audio zone configurations with per-era stem URLs and overlap widths"
      exports: ["AUDIO_ZONES"]
  key_links:
    - from: "src/audio/audioMath.ts"
      to: "src/audio/audioZones.ts"
      via: "AudioZoneConfig type shared"
      pattern: "AudioZoneConfig"
---

<objective>
Create the pure-function audio math layer and 13-zone configuration for scroll-reactive audio mixing.

Purpose: Establish the testable foundation for audio crossfading before introducing Tone.js. All gain computation logic is pure TypeScript with no browser or Web Audio dependency, enabling full unit testing in the node Vitest environment.

Output: audioMath.ts (gain computation), audioMath.test.ts (comprehensive tests), audioZones.ts (13 era configs)
</objective>

<execution_context>
@/Users/tsaylor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tsaylor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audio-engine/03-RESEARCH.md

@src/config/eras.ts
@src/store/eraStore.ts

<interfaces>
<!-- From src/config/eras.ts — era IDs and names used to define audio zones -->
```typescript
export interface EraConfig {
  id: number;       // 0-indexed (0-12)
  name: string;
  weight: number;   // relative scroll distance
}
export const ERA_CONFIG: EraConfig[];  // 13 entries, id 0-12
```

<!-- From src/store/eraStore.ts — scroll state that drives audio mixing -->
```typescript
export interface EraState {
  globalProgress: number;   // 0-1 across entire scroll
  currentEra: number;       // 0-12 integer
  eraProgress: number;      // 0-1 within current era
  isScrollEnabled: boolean;
}
export const eraStore: StoreApi<EraState>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audio math — gain computation with TDD</name>
  <files>src/audio/audioMath.ts, src/audio/audioMath.test.ts</files>
  <behavior>
    - computeGainForEra(eraIndex=5, currentEra=5, eraProgress=0.5, overlapWidth=0.1) returns 1.0 (active era, past overlap)
    - computeGainForEra(eraIndex=5, currentEra=5, eraProgress=0.05, overlapWidth=0.1) returns 0.5 (active era, mid-overlap fade-in)
    - computeGainForEra(eraIndex=4, currentEra=5, eraProgress=0.05, overlapWidth=0.1) returns 0.5 (previous era, mid-overlap fade-out)
    - computeGainForEra(eraIndex=4, currentEra=5, eraProgress=0.5, overlapWidth=0.1) returns 0.0 (previous era, past overlap)
    - computeGainForEra(eraIndex=3, currentEra=5, eraProgress=0.5, overlapWidth=0.1) returns 0.0 (non-adjacent era)
    - computeGainForEra(eraIndex=0, currentEra=0, eraProgress=0.0, overlapWidth=0.1) returns 0.0 (first era at exact start — overlap fade-in begins at 0)
    - computeGainForEra(eraIndex=0, currentEra=0, eraProgress=0.1, overlapWidth=0.1) returns 1.0 (first era, overlap complete)
    - overlapWidth=0 edge case: no fade, immediate switch (gain is 1 or 0)
    - Gain values are always clamped to [0, 1]
  </behavior>
  <action>
    Write tests FIRST in src/audio/audioMath.test.ts covering all behaviors above. Run tests (expect RED). Then implement src/audio/audioMath.ts with:

    1. Export `AudioZoneConfig` interface: `{ eraIndex: number; stemUrl: string; overlapWidth: number; }`
    2. Export `computeGainForEra(eraIndex: number, currentEra: number, eraProgress: number, overlapWidth: number): number`
       - If eraIndex === currentEra: fade in over overlap region (eraProgress / overlapWidth), clamped to 1.0 beyond overlap
       - If eraIndex === currentEra - 1: fade out (1 - eraProgress / overlapWidth), clamped to 0.0 beyond overlap
       - All other eras: return 0
       - Clamp all results to [0, 1] range
       - When overlapWidth is 0: currentEra gets 1.0, all others get 0.0 (avoid division by zero)

    Per AUDO-03: This function returns the TARGET gain value. The AudioEngine (Plan 02) will pass this value to `gain.gain.rampTo(target, 0.05)` — never direct `.value` assignment. Document this contract in a JSDoc comment on the function.

    Run tests again (expect GREEN).
  </action>
  <verify>
    <automated>cd /Users/tsaylor/projects/somewhere-between-then-and-now && npm run test -- src/audio/audioMath.test.ts</automated>
  </verify>
  <done>All gain computation tests pass. computeGainForEra handles active era, previous era fade-out, non-adjacent silence, overlap boundaries, zero-overlap edge case, and clamp to [0,1].</done>
</task>

<task type="auto">
  <name>Task 2: Audio zone configuration for 13 eras</name>
  <files>src/audio/audioZones.ts</files>
  <action>
    Create src/audio/audioZones.ts exporting `AUDIO_ZONES: AudioZoneConfig[]` with 13 entries (one per era, matching ERA_CONFIG ids 0-12).

    Import `AudioZoneConfig` from `@/audio/audioMath`.

    For each zone:
    - `eraIndex`: 0-12 matching ERA_CONFIG
    - `stemUrl`: `/audio/era-${String(id).padStart(2, '0')}.mp3` (e.g., `/audio/era-00.mp3`)
    - `overlapWidth`: default 0.1 (10% of era) for all eras initially. Per research open question #2, this is a per-era authorial tuning value — using uniform default now.

    Add a JSDoc comment noting that overlapWidth values are scaffold defaults for authorial tuning later, mirroring how ERA_CONFIG weights work.

    NOTE: The actual .mp3 files do NOT need to exist. AUDO-06 (Plan 02) ensures graceful fallback when stems are missing.
  </action>
  <verify>
    <automated>cd /Users/tsaylor/projects/somewhere-between-then-and-now && npx tsc --noEmit src/audio/audioZones.ts 2>&1 || echo "TypeScript check done"</automated>
  </verify>
  <done>AUDIO_ZONES array has exactly 13 entries with eraIndex 0-12, stemUrl paths, and overlapWidth values. Types align with AudioZoneConfig from audioMath.ts.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/audio/audioMath.test.ts` — all gain computation tests pass
- AUDIO_ZONES has 13 entries with correct eraIndex values 0-12
- No browser/DOM dependencies in any file (pure node-testable)
</verification>

<success_criteria>
- computeGainForEra correctly computes gain for active, adjacent, and non-adjacent eras across all overlap scenarios
- 13 audio zone configs exist with stem URLs and overlap widths
- All tests green in vitest node environment
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-engine/03-01-SUMMARY.md`
</output>
