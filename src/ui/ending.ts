import type { Ending } from '../contracts';
import type { FrameState } from '../state';

/** STUB. Replaced by the ui unit. */
export function createEnding(_root: HTMLElement, _opts: { title: string }): Ending {
  return { update(_state: FrameState) {} };
}
